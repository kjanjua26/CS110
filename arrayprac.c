#include <stdio.h>
#include <math.h>
#include <string.h>
int main()
{
/*

    int data_list[5] = {1, 2, 3, 5, 7};

    for (int i = 0; i < 5; i++)
    {
        data_list[i] = i*i*i;
        printf("%d\n", data_list[i]);

    }
    fib(10);
    printf("\n\n");
    printf("%d", data_list[5]);
    return 0;
}
    int fib(int n)
{

    int F = alloc_array(int, n);
    F[0] = 0;
    F[1] = 1;
    for (int i = 0; i < n; i++)
    F[i+2] = F[i+1] + F[i];
    return F;

    */
    char s1 = "Hello World";
    printf("%s", strcat(s1, '\0'));

}
